<H1> All Tasks</H1>
<%# link_to xxxx prefix edit this!! %>
<% @tasks.each do |task| %>
<h5><%= link_to task.title, task_path(task.id) %> <%= task.details %></h5>
<p><%= task.completed %></p>
<%= link_to "Delete", task_path(task), method: :delete %>
<% end %>